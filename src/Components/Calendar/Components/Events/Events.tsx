import React from "react"
import { Box, Button, Card, Modal, Typography } from "@mui/material"

export const Events = () => {
  return <Card style={{ padding: "16px", margin: "64px 16px" }}>Trabalhando na tela de eventos</Card>
}
